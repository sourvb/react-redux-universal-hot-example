import React, {Component} from 'react';

export default class CounterButton extends Component {
  render() {
    return (
      <button>
        You have clicked me time.
      </button>
    );
  }
}
